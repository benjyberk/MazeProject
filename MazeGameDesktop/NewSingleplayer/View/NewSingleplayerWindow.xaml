<Window x:Class="MazeGameDesktop.NewSingleplayer.NewSingleplayerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MazeGameDesktop.NewSingleplayer"
        xmlns:shared="clr-namespace:MazeGameDesktop"
        DialogResult="{Binding Close}"
        mc:Ignorable="d"
        Title="NewSingleplayerWindow" Height="200" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="Choose Settings For Your Maze" FontWeight="Bold" HorizontalAlignment="Center"/>
        <shared:GameSettings x:Name="Form" Grid.Row="1"/>
    </Grid>
</Window>
